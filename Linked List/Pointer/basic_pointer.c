#include <stdio.h>
int main(){
    int x;
    x=10;
    printf("%d\n",x);//output be 10 becuse it's printing x value.
    printf("%p\n",&x);// this will be print memory location . '&' for this symbol memory address will show.
    return 0;
}
